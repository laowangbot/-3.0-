# AWS Lightsail部署环境变量配置模板
# 使用说明：复制此文件并重命名为 .env.机器人名称（例如：.env.transfer_bot1）
# 然后填入对应的配置信息

# ==================== 基本配置 ====================
# 机器人标识（建议格式：transfer_bot1, member_bot1等）
BOT_ID=bot_name_here
BOT_NAME=机器人显示名称

# ==================== Telegram配置 ====================
# 机器人Token（从@BotFather获取）
# 格式：123456789:ABCdefGHIjklMNOpqrsTUVwxyz
BOT_TOKEN=your_bot_token_here

# Telegram API配置（从 https://my.telegram.org 获取）
API_ID=your_api_id_here
API_HASH=your_api_hash_here

# ==================== Firebase配置 ====================
# Firebase项目ID
FIREBASE_PROJECT_ID=your_firebase_project_id

# Firebase服务账号凭据（完整JSON格式，单行）
# 格式：{"type":"service_account","project_id":"...","private_key":"..."}
FIREBASE_CREDENTIALS={"type":"service_account","project_id":"your_project_id","private_key_id":"your_private_key_id","private_key":"-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY\n-----END PRIVATE KEY-----\n","client_email":"your_service_account@your_project.iam.gserviceaccount.com","client_id":"your_client_id","auth_uri":"https://accounts.google.com/o/oauth2/auth","token_uri":"https://oauth2.googleapis.com/token","auth_provider_x509_cert_url":"https://www.googleapis.com/oauth2/v1/certs","client_x509_cert_url":"https://www.googleapis.com/oauth2/v1/certs/your_service_account%40your_project.iam.gserviceaccount.com","universe_domain":"googleapis.com"}

# ==================== 存储配置 ====================
# 存储模式：
# - true: 使用本地存储（推荐用于AWS部署，避免Firebase配额限制）
# - false: 使用Firebase存储
USE_LOCAL_STORAGE=true

# ==================== 日志配置 ====================
# 日志级别：DEBUG, INFO, WARNING, ERROR
# 推荐：生产环境使用INFO，调试时使用DEBUG
LOG_LEVEL=INFO

# ==================== 端口配置（AWS不需要，但保留兼容性）====================
PORT=8092

# ==================== Render配置（AWS不需要，但保留兼容性）====================
RENDER_EXTERNAL_URL=http://localhost:8092

# ==================== Firebase优化配置 ====================
# 启用Firebase批量存储（如果使用Firebase）
FIREBASE_BATCH_ENABLED=true

# 批量存储间隔（秒）
FIREBASE_BATCH_INTERVAL=300

# 最大批量大小
FIREBASE_MAX_BATCH_SIZE=100

# ==================== 配置说明 ====================
# 1. BOT_TOKEN获取方法：
#    - 在Telegram搜索 @BotFather
#    - 发送 /newbot 创建新机器人
#    - 或发送 /token 获取现有机器人的token
#
# 2. API_ID和API_HASH获取方法：
#    - 访问 https://my.telegram.org
#    - 登录您的Telegram账号
#    - 进入 "API development tools"
#    - 创建应用获取 api_id 和 api_hash
#
# 3. Firebase配置（可选）：
#    - 如果使用本地存储（USE_LOCAL_STORAGE=true），可以不配置Firebase
#    - 如果需要Firebase：
#      a. 访问 https://console.firebase.google.com
#      b. 创建项目
#      c. 项目设置 → 服务账号 → 生成新的私钥
#      d. 下载JSON文件，将内容转为单行填入FIREBASE_CREDENTIALS
#
# 4. Session文件：
#    - 确保对应的 .session 文件已上传到 sessions/ 目录
#    - Session文件名应与BOT_ID对应（例如：transfer_bot1.session）
#
# 5. 多机器人部署：
#    - 为每个机器人创建独立的配置文件
#    - 配置文件命名：.env.transfer_bot1, .env.transfer_bot2, .env.member_bot1等
#    - 每个机器人使用不同的BOT_TOKEN和BOT_ID
#    - 可以共用相同的API_ID、API_HASH和Firebase配置

# ==================== 安全提示 ====================
# ⚠️ 注意：
# 1. 不要将.env文件提交到Git仓库（已在.gitignore中排除）
# 2. 妥善保管BOT_TOKEN，泄露后需立即重置
# 3. Session文件包含登录凭证，注意保护
# 4. Firebase私钥包含敏感信息，不要公开
# 5. 设置文件权限：chmod 600 .env.*








