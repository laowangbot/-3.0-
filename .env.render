# Render生产环境配置示例
# 在Render控制台中设置这些环境变量

# 生产环境机器人配置
BOT_ID=your_production_bot_id
BOT_NAME=生产环境机器人
API_ID=your_production_api_id
API_HASH=your_production_api_hash
BOT_TOKEN=your_production_bot_token

# Render配置
PORT=8080
RENDER_EXTERNAL_URL=https://your-app-name.onrender.com

# Firebase配置（生产环境）
FIREBASE_PROJECT_ID=your_firebase_project_id
FIREBASE_CREDENTIALS=your_firebase_credentials_json

# Render部署说明：
# 1. 在Render控制台的Environment Variables中设置上述变量
# 2. 不要在代码中硬编码这些值
# 3. FIREBASE_CREDENTIALS需要是完整的JSON字符串（压缩成一行）
# 4. 确保BOT_TOKEN对应的机器人有正确的权限