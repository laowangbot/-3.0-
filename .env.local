# 本地开发环境配置
# 复制此文件为 .env 并填入你的本地配置信息

# 本地机器人配置
BOT_ID=your_local_bot_id
BOT_NAME=本地测试机器人
API_ID=your_local_api_id
API_HASH=your_local_api_hash
BOT_TOKEN=your_local_bot_token

# 本地端口（可选）
PORT=8080

# Firebase配置（本地和Render可以使用相同的Firebase项目）
FIREBASE_PROJECT_ID=your_firebase_project_id
FIREBASE_CREDENTIALS=your_firebase_credentials_json

# 注意事项：
# 1. 本地和Render可以使用不同的BOT_TOKEN和API_ID
# 2. Firebase配置可以共享，也可以分别使用不同的项目
# 3. 请确保.env文件不要提交到Git仓库中